import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ChiamamiProfessionista',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
